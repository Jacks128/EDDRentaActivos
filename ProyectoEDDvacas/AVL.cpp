#include "AVL.h"

/*AVL::AVL() {
	this->root = nullptr;
}*/

/*void AVL::AddToCompare(Activos* activos, NodoAVL* temp)
{
	int valor;
	valor = activos->getNombreActivo.compare(temp->getActivos()->getNombreActivo);
	if (valor == -1)
	{
		if (temp->left != 0)
		{
			AVL::AddToCompare(activos, temp->left);
		}
		else
		{
			temp->left = new NodoAVL(new Activos(*activos));
		}
	}else{
		if (temp->right != 0)
		{
			AVL::AddToCompare(activos, temp->right);
		}  
		else
		{
			temp->right = new NodoAVL(new Activos(*activos));
		}
	}
}*/



/*void AVL::AddNodo(NodoAVL* nodo) 
{
	if (nodo != 0) {

	}
}*/
 